import 'package:flutter/material.dart';

class AddressElement extends StatelessWidget {
  const AddressElement({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
